torch>=2.1.0
torchvision
datasets>=2.20.0
pyarrow>=14.0.0
huggingface_hub
hf-transfer
einops
omegaconf
wandb
Pillow
tqdm
torchmetrics[image]
# transformers + its full torch backend.
# The [torch] extra pulls tokenizers, safetensors, and accelerate at compatible
# versions; without it, torchmetrics' eager text-submodule import crashes with
# `Could not import module 'AutoModel'`.
transformers[torch]>=4.46.0
tokenizers>=0.20.0
safetensors>=0.4.5
sentencepiece>=0.2.0
protobuf>=3.20.0
accelerate>=0.34.0
google-cloud-storage

# Cosmos tokenizer (NVIDIA) — only needed when use_tokenizer=true. Not on PyPI;
# installed straight from GitHub. Pulls a few extra deps (mediapy, loguru, etc.).
cosmos-tokenizer @ git+https://github.com/NVIDIA/Cosmos-Tokenizer.git
